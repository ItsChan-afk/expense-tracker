/*
const mongoose = require('mongoose')

const TransactionSchema = await mongoose.Schema({
    name : {
        type: String,
        required : true
    },
    description : {
        type: String,
        required : true, 
    },
})

*/